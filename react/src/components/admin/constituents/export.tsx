import { Button, Checkbox, FormControlLabel, FormGroup, Link } from "@mui/material"
import { pink } from "@mui/material/colors"
import { Box } from "@mui/system"
import { FC } from "react"

const Export: FC = () => {
    return (
        <>
            <Box>
                <p>Экспорт:</p>
                {/* <Button variant="contained" size="large" onClick={sendExport()}>
                    Экспортировать
                </Button> */}
            </Box>
        </>
    )
}

export default Export